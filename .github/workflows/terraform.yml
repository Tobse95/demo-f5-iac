name: Terraform

on:
  pull_request:
    paths: ["*/subnets/**"]
  push:
    branches: [main]
    paths: ["*/subnets/**"]

permissions:
  contents: read
  pull-requests: write

jobs:
  terraform:
    name: Plan / Apply all devices
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.0"

      - name: Detect affected device folders
        id: devices
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BASE=${{ github.event.pull_request.base.sha }}
            HEAD=${{ github.event.pull_request.head.sha }}
            DIRS=$(git diff --name-only $BASE...$HEAD | grep '/subnets/' | cut -d/ -f1 | sort -u)
          else
            DIRS=$(git diff --name-only HEAD~1..HEAD | grep '/subnets/' | cut -d/ -f1 | sort -u)
          fi
          echo "dirs<<EOF" >> $GITHUB_OUTPUT
          echo "$DIRS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          echo "Affected devices: $DIRS"

      - name: Terraform plan (PR)
        id: plan
        if: github.event_name == 'pull_request'
        run: |
          FULL_OUTPUT=""
          while IFS= read -r dir; do
            [ -z "$dir" ] && continue
            echo "=== Planning: $dir ==="
            cd "$dir"
            terraform init -input=false -no-color
            OUTPUT=$(terraform plan -no-color -input=false 2>&1)
            FULL_OUTPUT="${FULL_OUTPUT}\n#### ${dir}\n\`\`\`\n${OUTPUT}\n\`\`\`"
            cd ..
          done <<< "${{ steps.devices.outputs.dirs }}"
          echo "plan_output<<ENDOFPLAN" >> $GITHUB_OUTPUT
          echo -e "$FULL_OUTPUT" >> $GITHUB_OUTPUT
          echo "ENDOFPLAN" >> $GITHUB_OUTPUT

      - name: Post plan comment
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const body = `### F5 Terraform Plan\n\n${{ steps.plan.outputs.plan_output }}\n\n*Triggered by @${{ github.actor }}*`;
            github.rest.issues.createComment({
              owner: context.repo.owner, repo: context.repo.repo,
              issue_number: context.issue.number, body
            });

      - name: Terraform apply (merge)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          while IFS= read -r dir; do
            [ -z "$dir" ] && continue
            echo "=== Applying: $dir ==="
            cd "$dir"
            terraform init -input=false -no-color
            terraform apply -auto-approve -input=false -no-color
            cd ..
          done <<< "${{ steps.devices.outputs.dirs }}"

      - name: Signal deployment to self-service
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ secrets.SELF_SERVICE_GITHUB_TOKEN }}
        run: |
          while IFS= read -r dir; do
            [ -z "$dir" ] && continue
            for yaml in "$dir/subnets/"*.yaml; do
              [ -f "$yaml" ] || continue
              name=$(grep '^subnet_name:' "$yaml" | awk '{print $2}' | tr -d '"')
              env=$(grep '^environment:' "$yaml" | awk '{print $2}' | tr -d '"')
              [ -z "$name" ] || [ -z "$env" ] && continue
              gh api repos/Tobse95/network-self-service/dispatches \
                --method POST \
                -f event_type=subnet-active \
                -f "client_payload[subnet_name]=$name" \
                -f "client_payload[environment]=$env" \
                -f "client_payload[platform]=f5" || true
            done
            break  # signal once; all devices share the same subnets
          done <<< "${{ steps.devices.outputs.dirs }}"
